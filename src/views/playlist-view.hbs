{{> menu}}

<section class="section">
  <div class="level">
    <div class="level-left">
      <h1 class="title">{{playlist.title}}</h1>
    </div>
    <div class="level-right">
      <a href="/dashboard/deleteplaylist/{{playlist._id}}" class="button is-danger is-small">
  Delete playlist
    </a>
    </div>
  </div>

  {{> list-tracks}}
  {{> add-track}}
</section>
